name: MSBuild
on: push
jobs:
  build:
    name: MSBuild
    runs-on: self-hosted
    steps:
    - name: Check cmd
      shell: cmd
      run: |
        echo "############"
        echo "cmd OK!"
        echo "############"
        chdir
        MSBuild
