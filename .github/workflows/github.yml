name: MSBuild
on: push
jobs:
  build:
    name: MSBuild
    runs-on: self-hosted
    steps:
    - name: run multi command
      shell: cmd
      run: |
        echo "############"
        echo "Hello World!"
        echo "############"